#!/bin/bash
f="$(find -L $CBI/desktop-artwork/wallpapers/ -name \*.png -o -name \*.jpg |shuf -n1)"
echo "[`date`] FEH BACKGROUND: $f" >> ~/.xlog
feh --bg-fill $f

if [ "$1" ]; then
		while true
		do
				sleep 10
				`${BASH_SOURCE[0]}`
		done
fi
