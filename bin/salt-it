#!/bin/bash
DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"/../
export GNUPGHOME="$(gpgconf --list-dirs | sed -nre 's/homedir://p')"

(cd $DIR && 
git-verify-commit-signature HEAD &&
if [ "$1" ]; then
sudo -E salt-call state.sls "$1"
else
sudo -E salt-call state.highstate
fi		
)
